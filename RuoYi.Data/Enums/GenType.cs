using System.ComponentModel;

namespace RuoYi.Data.Enums
{
    // 生成代码方式（0zip压缩包 1自定义路径）
    public enum GenType
    {
        [Description("zip压缩包")]
        Zip = 0,
        [Description("自定义路径")]
        CustomPath = 1
    }
}
